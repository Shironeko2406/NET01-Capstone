namespace ClinictManagementSystem.Enums
{
    public enum MedicineUnitTypeEnum
    {
        Box,     // Hộp (Box)
        Strip,   // Vỉ (Strip/Blister)
        Bottle,  // Chai (Bottle)
        Tablet,  // Viên (Tablet/Pill)
        Tube,    // Tuýp (Tube)
        Other    // Khác (Other)
    }
}
